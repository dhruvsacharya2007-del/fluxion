services:
  db:
    image: postgres:17
    restart: unless-stopped
    environment:
      POSTGRES_USER: fluxion
      POSTGRES_PASSWORD: fluxion
      POSTGRES_DB: fluxion
    ports:
      - '5433:5432'
    volumes:
      - fluxion_pgdata:/var/lib/postgresql/data

volumes:
  fluxion_pgdata: